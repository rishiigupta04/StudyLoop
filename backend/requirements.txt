# Runtime deps only — keep this lean (Render free tier ~512 MB RAM). No torch in prod.
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic-settings>=2.7,<3
httpx==0.28.*
python-multipart==0.0.*   # /api/stt audio uploads (hosted speech-to-text, D14)
PyJWT[crypto]==2.*
langgraph>=1.0,<1.2
# Postgres checkpointer (CHECKPOINTER=postgres); pure-Python + a small binary wheel, no torch
langgraph-checkpoint-postgres>=3,<4
psycopg[binary]>=3.2,<4
psycopg-pool>=3.2,<4
